<table class="table table-hover">
    <thead>
    <tr>
        <th><h3>Amigos</h3></th>
        <th colspan="2"></th>
    </tr>
    </thead>

    <tbody>
    <% @friends.each do |friend| %>
        <tr>
            <td><%= User.find(friend.friend_id).name %></td>
            <td>
                <%= link_to 'Mostrar', user_path(id: User.encrypt(friend.friend_id)), class: 'btn btn-success' %>
                <%= link_to 'Cancelar amizade', cancel_friend_path(friend_id: User.encrypt(friend.friend_id)), method: :delete, data: {confirm: 'Você tem certeza?'}, class: 'btn btn-danger' %>
            </td>
        </tr>
    <% end %>
    </tbody>
</table>