<img src=<%= GravatarApi.mid_photo(user.email) %> class="thumbnail">

<%= link_to user.name, user %>

<br>

<% if current_user.admin %>
    <%= link_to "Cancelar conta", destroy_user_path(user), :data => {:confirm => "Você tem certeza?"}, :method => :delete, :class => 'btn btn-danger' %>
<% end %>
