<h4><span class="glyphicon glyphicon-globe"></span> Perfil Público</h4>

<div class="row">
    <div class="col-md-3">
        <br>
        <img src=<%= @gravatar %> class="thumbnail">
        <br>
    </div>

    <div class="col-md-9">
        <br>
        <h2><%= @user.name if @user.name %></h2>
        <h5><%= @user.email if @user.email %></h5>
        <h5><%= link_to "Adicionar amigo", add_friend_path, {class: "btn btn-primary"} %></h5>
    </div>
</div>

<br>

<%= render :partial => 'userdata/userdata_list' %>

<br>

<%= render :partial => 'reviews/review_list' %>

<br>

<%= render :partial => 'users/friends_list' %>
