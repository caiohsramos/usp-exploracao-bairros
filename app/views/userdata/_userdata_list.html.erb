<h4><span class="glyphicon glyphicon-bookmark"></span> Locais Favoritos</h4>
<table class="table table-hover">
    <thead>
    <tr>
        <th colspan="1"></th>
        <th>Local</th>
        <% if @user == current_user %>
            <th>Comentário Privado</th>
        <% else %>
            <th colspan="3"></th>
        <% end %>
        <th colspan="4"></th>
    </tr>
    </thead>

    <tbody>
    <% @userdata.each do |userdatum| %>
        <tr>
            <td width="50"><img src=<%= GoogleApi.place_details(userdatum.place_id)['icon'] %>></td>
            <td><%= link_to userdatum.name, userdatum %></td>

            <% if current_user.id == userdatum.user_id %>
                <td><%= userdatum.comment %></td>
                <td class="text-right">
                    <%= link_to 'Editar', edit_userdatum_path(userdatum), class: 'btn btn-warning' %>
                    <%= link_to 'Apagar', userdatum, method: :delete, data: {confirm: 'Você tem certeza?'}, class: 'btn btn-danger' %>
                </td>
            <% else %>
                <th colspan="3"></th>
            <% end %>
        </tr>
    <% end %>
    </tbody>
</table>